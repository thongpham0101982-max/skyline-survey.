const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const items = [{ code: 'DOT_LE', name: 'Dot Le', categoryType: 'KY_KS', sortOrder: 1 }, { code: 'OPEN_DAY', name: 'Open Day', categoryType: 'KY_KS', sortOrder: 2 }]; for (const i of items) { const exists = await prisma.assessmentConfig.findFirst({ where: { categoryType: i.categoryType, code: i.code } }); if (!exists) { await prisma.assessmentConfig.create({ data: i }); console.log('Created', i.code); } else { console.log('Exists', i.code); } } } main().catch(e => console.error(e)).finally(() => prisma.$disconnect()); 
