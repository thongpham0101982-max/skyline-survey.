const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const assigns = await prisma.inputAssessmentTeacherAssignment.findMany({ include: { subject: true, user: true } }); console.log(assigns.map(a => a.user.fullName + ' - ' + a.subject.name + ' - Kh?i ' + a.grade + ' - ' + a.educationSystem)); } main().catch(console.error).finally(() => prisma.$disconnect());
