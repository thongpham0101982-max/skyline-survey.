# 03. IMPLEMENTATION SPECIFICATION (PATCH v1.0.1)

---

### 1. NGUYÊN TẮC TRIỂN KHAI TINH GỌN (MINIMAL SURFACE AREA)
- Tái sử dụng component `Input` và `DataTable` chuẩn từ `src/components/ui/`.
- Không tạo component mới; chỉ thêm keyboard navigation listener vào form nhập điểm.
- Kiểm thử cô lập trên Staging trước khi phát hành.
