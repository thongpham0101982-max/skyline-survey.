const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const st = await prisma.inputAssessmentStudent.findMany({ where: { periodId: 'cmnrk8tcd00019yj31f18hjql' } }); console.log(st.map(s => s.fullName + ' - grade: ' + s.grade + ' - surveySystem: ' + s.surveySystem + ' - surveyFormType: ' + s.surveyFormType + ' - batchId: ' + s.batchId)); } main().catch(console.error).finally(() => prisma.$disconnect());
